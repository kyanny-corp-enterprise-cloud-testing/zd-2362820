name: rulesets-required-workflow-1

on:
  pull_request:
    types:
      - opened
      - edited
      - reopened
      - sychronize
      - ready_for_review

jobs:
  rulesets-required-workflow-1-job-1:
    if: github.event.pull_request.draft == false && github.event.pull_request.state == 'open'
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello, world!
